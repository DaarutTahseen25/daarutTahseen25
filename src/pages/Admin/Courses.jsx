import React from "react";
import DashTitle from "../../Components/DashTitle";

const Courses = () => {
  return (
    <div>
      <DashTitle
        title="Courses Management"
        subtitle="Add, edit, and monitor all available courses"
      />
    </div>
  );
};

export default Courses;
