import React from "react";
import DashTitle from "../../Components/DashTitle";

const Tests = () => {
  return (
    <div>
      <DashTitle
        title="Tests Management"
        subtitle="Create, edit, and monitor all aptitude tests and results"
      />
    </div>
  );
};

export default Tests;
